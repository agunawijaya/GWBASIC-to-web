Hi, welcome to the Classic Old DOS Games collection.

You might wish to print out this file and have it beside you.

FILE ASSOCIATIONS

If you don't know how to access these programs which have the extension BAS and you are using Windows 95 or 98 you can do the following:

Open up the directory which contains these programs using Explorer and, holding down the SHIFT key, right click on any of the files with the extension BAS (ie anatomy.bas). 

In the context menu which appears you will see an "Open with..." command. 

Click on that and a small dialog box will open up on the screen. 

At the bottom of this dialog box is a button labelled "Other" which you should click on. 

There will be a "browsing" dialog box open up and you must find the file GW.EXE and click on that. 

You will be returned to the previous dialog box and, before clicking "OK", make sure that the "Always use this program to open this type of file" box is selected. 

Click on the "OK" button and, from now on, every time you double click on any file with extension BAS it will load it into GW.EXE (GWBasic).


BAT FILES

You can also automate the loading of GW.EXE and then the BAS file by creating a BAT file (a pure text file with the extension BAT) in NotePad or some other ASCII text editor and placing in that text file the following information:


@echo off
gw temple.bas


(where "temple.bas" is the name of the BAS file)

Save that text file with the BAT extension (usually call the file by the name of the program, ie the "temple.bas" program goes into a BAT file called "temple.bat").

You will find a few BAT files in the collection and you may open up one of those in NotePad or some other ASCII text editor (not in a normal word processor unless you can save the file as an ASCII text file) and make the appropriate amendments and then save it as the new program name (with a BAT extension).



We hope this helps you to access these files. Remember, there is no warranty, no support, no back-up, and no refunds! YOU ARE ON YOUR OWN!!! Good luck.
